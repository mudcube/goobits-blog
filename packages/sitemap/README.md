# @goobits/sitemap

Reusable sitemap core and XML helpers for SvelteKit sites.

This package owns generic route entry types, filter/sort logic, and XML/origin helpers.
Host apps continue to own route scanning policy, category mapping, and page presentation.
