{
  "name": "@calendar/kit",
  "version": "0.0.0",
  "description": "Cross-cutting infra for the calendar federation — HTTP helpers, runtime/build env, dev D1 shim, auth adapters.",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src",
    "tsconfig.json",
    "README.md"
  ],
  "peerDependencies": {
    "@sveltejs/kit": "^2.0.0",
    "svelte": "^5.0.0"
  },
  "dependencies": {
    "@calendar/migrations": "workspace:*",
    "@goobits/auth": "workspace:*",
    "better-sqlite3": "^12.10.0"
  },
  "devDependencies": {
    "@sveltejs/kit": "^2.60.1"
  }
}
