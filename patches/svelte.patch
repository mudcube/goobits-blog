diff --git a/src/internal/client/dom/operations.js b/src/internal/client/dom/operations.js
index 4036aa2d61bb5346b330e8e60b58aa38d22271b1..2b7c433e08f73a28f3076ac8e5d2fdfea34e9481 100644
--- a/src/internal/client/dom/operations.js
+++ b/src/internal/client/dom/operations.js
@@ -41,10 +41,22 @@ export function init_operations() {
 	var node_prototype = Node.prototype;
 	var text_prototype = Text.prototype;
 
+	// Some browsers/environments can expose these without an accessor getter (Safari observed).
+	// Fall back to direct property access so hydration can't crash.
 	// @ts-ignore
-	first_child_getter = get_descriptor(node_prototype, 'firstChild').get;
+	first_child_getter =
+		get_descriptor(node_prototype, 'firstChild').get ??
+		function () {
+			// @ts-ignore
+			return this.firstChild;
+		};
 	// @ts-ignore
-	next_sibling_getter = get_descriptor(node_prototype, 'nextSibling').get;
+	next_sibling_getter =
+		get_descriptor(node_prototype, 'nextSibling').get ??
+		function () {
+			// @ts-ignore
+			return this.nextSibling;
+		};
 
 	if (is_extensible(element_prototype)) {
 		// the following assignments improve perf of lookups on DOM nodes
